/**
 * Public config for GitHub Pages / Vercel hosting.
 * 1) Deploy API: vercel.com → Import this repo → add env BAKONG_TOKEN + BAKONG_EMAIL
 * 2) Paste your Vercel URL below (no secrets here — JWT stays on Vercel only).
 */
window.DYNA_BAKONG_CONFIG = {
  /** e.g. https://dyna-store-xxxx.vercel.app */
  apiBase: '',
  account: 'ben_sothida@bkrt',
  organization: 'Dyna Store',
  project: 'dyna_store',
}
